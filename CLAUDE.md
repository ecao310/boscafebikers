# CLAUDE.md — Boston Café Bikers

Static site + ICS sync for a casual Boston cycling group that rides to cafés.
Tagline: "exploring the city one café at a time".

## File map

| Path | Purpose |
| --- | --- |
| `ralph/PROMPT.md` | The Ralph loop spec (project spec + rules + backlog source of truth). |
| `ralph/ralph.log` | Gitignored loop scratch — per-iteration notes (do not commit). |
| `ralph/PLAN.md` | Working copy of the backlog. Mark tasks `[x]`; keep the bullet at its original wording (notes go in `ralph/ralph.log`). |
| `CLAUDE.md` | This file — decisions, gotchas, conventions for the next iteration. |
| `requirements.txt` | Python deps: `icalendar`, `requests`, `pytest`. |
| `scripts/fetch_rides.py` | Fetches + parses the ICS feed → `site/events.json`; enriches ride images from public Partiful pages on live syncs. |
| `scripts/promote_events.py` | Copies fetched JSON over the committed one only if `events` differ. |
| `scripts/archive_events.py` | Accumulates already-happened rides into `site/events-past.json` (stdlib only). |
| `scripts/enrich_archive.py` | Backfills images/routes onto archived rides, a few per sync run. |
| `scripts/route_map.py` | Draws a ride's route as a self-contained SVG (stdlib only, no network). |
| `scripts/render_route_maps.py` | Fetches route geometry + OSM tiles and writes `site/maps/<uid>.svg`; sets `map_image`. |
| `scripts/ride_images.json` | Optional sidecar: ride UID → image URL; an explicit entry wins over auto-enrichment. |
| `tests/fixtures/sample.ics` | Offline fixture (2 future, 1 past, 1 cancelled). |
| `tests/fixtures/event-page.html` | Offline fixture: a Partiful event page (`__NEXT_DATA__`) — image + `customFields` route links. |
| `tests/fixtures/tile.png` | Offline 8×8 PNG standing in for a basemap tile in `test_route_map.py`. |
| `tests/test_fetch_rides.py` | pytest suite for the fetch script (offline only). |
| `tests/test_archive_events.py` | pytest suite for the past-rides archive merge. |
| `tests/test_route_map.py` | pytest suite for the route-map projection, SVG, and renderer. |
| `tests/test_enrich_archive.py` | pytest suite for the bounded archive backfill. |
| `site/styles.css` | Shared stylesheet: café palette + base/nav/hero/footer, linked by every page. |
| `site/index.html` | Home: rides calendar, page-specific inline CSS + shared `styles.css`, three ride JS files in dependency order. |
| `site/js/ride-card.js` | `window.BCB` module: `rideCard()` builder, `.ics`/Google Calendar exports, shared constants (`PARTIFUL`, `MONTHS`, `DOW`), `el()` helper. Loaded first. |
| `site/js/calendar.js` | `window.BCB` module: FullCalendar renderer + fallback month grid + Eastern wall-clock date math. Loaded second. |
| `site/js/app.js` | `window.BCB` module: bootstrap — `fetch`, render, next-ride, modal, "Last updated" stamp, deferred `data-bg` backgrounds. Loaded last. |
| `site/js/contact.js` | Standalone (no `BCB`): turns `contact.html`'s form into a prefilled `mailto:`. |
| `site/images/*.jpeg` | Six optimized photos (1600px longest side, `sips -Z 1600` quality-70); five ride photos also appear in `gallery.html`. |
| `site/gallery.html` | Ride-photo gallery: responsive grid + Instagram CTA; gallery CSS inline. |
| `site/shopify.html` | WIP shop page (placeholder only). |
| `site/meta-business.html` | WIP Meta Business page (placeholder only). |
| `site/contact.html` | Contact page: mailto-composing email form + the three contact cards. |
| `site/donate.html` | WIP donate page (placeholder only). |
| `site/events.json` | Generated ride data — **upcoming only** (committed; the workflow updates it). |
| `site/events-past.json` | Accumulating archive of rides that already happened; the calendar shows them dimmed. |
| `site/maps/*.svg` | Generated route maps, one per ride, OSM tiles embedded (committed; the sync draws the missing ones). |
| `.github/workflows/sync.yml` | Cron sync every 6h + manual dispatch. |
| `.github/workflows/pages.yml` | Assembles the Pages artifact (`master` at root + `dev` under `/preview/`) and deploys it. |
| `README.md` | Human-facing: how it works, ICS URL, secret, Pages deploy, local dev. |

## Conventions & decisions

- **Never commit the ICS URL.** It lives only in env var / GitHub secret `PARTIFUL_ICS_URL`. The script must never print it (including inside error messages — strip URLs from exception text).
- **Tests never hit the network.** Always parse `tests/fixtures/sample.ics`.
- Timezone for all displayed times: `America/New_York` (use `zoneinfo`).
- `site/` is plain static: no bundler, no build step. CSS split between shared `site/styles.css` (palette + base/nav/hero/footer — the single source for the café custom properties) and a small page-specific inline `<style>` per page. Mobile-first, warm café palette, readable at 380px. **One** exception to "no frameworks": the calendar view adopts FullCalendar 6 (MIT) from a CDN with `defer`, with a hand-rolled fallback.
- Public Partiful profile (safe to commit, empty-state fallback): `https://partiful.com/u/Hs47uq5mucZyXLBJZCda`
- Instagram: `@bostoncafebikers`.
- Python: stdlib + the three deps above only. Script exits nonzero on fetch/parse failure.

## Gotchas

- **Default branch is `master`** — locally and on GitHub. There is no `main`; ignore any tooling that assumes one. Workflows keyed on the default branch must say `master`.
- The remote is named **`boscafebikers`**, not `origin` (`git@github.com:ecao310/boscafebikers.git`). Every push/pull spells it out.
- **Headless Chrome screenshots at phone widths lie:** `--window-size=380,…` lays the page out at Chrome's ~500px minimum window width and crops the PNG. For a real 380px layout either iframe the page at width 380 inside a same-origin harness and crop, or drive Chrome over CDP (`--remote-debugging-port=0` + `Emulation.setDeviceMetricsOverride`; node's global `WebSocket` is enough) — the CDP route also lets you call `BCB.openRideModal(ev)` and screenshot the modal.
- `ralph/ralph.log` is gitignored loop scratch — do not commit it. Detailed per-iteration notes live there; keep CLAUDE.md a concise reference. All ralph files live in `ralph/`.

## Deployment

**Live: <https://ecao310.github.io/boscafebikers/>** (also `/events.json`). Everything headless.

- **Pages source: GitHub Actions**, not "deploy from a branch" (that source only offers `/` or `/docs`; the site lives in `site/`). Check with `gh api repos/:owner/:repo/pages` (`build_type` must be `"workflow"`); set it with `gh api -X PUT repos/:owner/:repo/pages -f build_type=workflow`. The leftover `source: {branch: master, path: "/"}` is ignored — don't fix it.
- **What deploys:** `.github/workflows/pages.yml` assembles `_site/` (see the preview bullet), uploads it via `actions/upload-pages-artifact` and publishes it with `actions/deploy-pages`.
- **Trigger chain — three ways in:**
  1. `push` on `master` (a human commit) → deploy.
  2. `sync.yml` (cron `0 */6 * * *` / dispatch) fetches, promotes `site/events.json` only if the rides changed, and if it committed, its `deploy` job calls `pages.yml` via `workflow_call` (bot commits made with `GITHUB_TOKEN` don't fire `push`).
  3. `workflow_dispatch` on `pages.yml`.
  4. `push` on `dev` → same deploy, but only the `/preview/` subtree changes.
- **Redeploy by hand:** `gh workflow run pages.yml --ref master` then `gh run watch <id> --exit-status` (~15s).
- **`dev` preview: <https://ecao310.github.io/boscafebikers/preview/>** (2026-08-22). A repo has exactly one Pages site, so `dev` gets no deployment of its own: every run of `pages.yml` builds **one artifact with two trees** — `master`'s `site/` at the root, `dev`'s `site/` under `preview/`. The root is always checked out from `master` whatever branch triggered the run, so a `dev` push cannot change the live site; no `dev` branch just means no preview. The artifact copy of each preview page gets a `<meta name="robots" content="noindex, nofollow">` injected after the **first** `<head>` (a plain substring swap would also hit `<header class="hero">`); the files on `dev` are untouched. The `github-pages` environment allow-lists deploying branches — `dev` was added with `gh api -X POST repos/:owner/:repo/environments/github-pages/deployment-branch-policies -f name=dev -f type=branch`; without it a `dev` push fails the environment gate. Preview only works because no URL in `site/` is root-relative.
- **Never use root-relative (`/…`) URLs in `site/`** — served from the `/boscafebikers/` subpath, so a leading slash 404s.
- **Sync→deploy gotchas (keep them):** `pages.yml` checks both branches out **by name** (`ref: master` / `ref: dev`), never `github.sha` — when `sync.yml` calls it, that SHA still points at the commit before the bot pushed `site/events.json`. sync's `deploy` job carries `contents: read / pages: write / id-token: write`; the actions (`actions/checkout@v4` et al.) target deprecated Node 20 and run on Node 24 — harmless today.
- **Calendar refresh/fetch backend (2026-08-13): keep the cron.** There is no public, unauthenticated Partiful endpoint that lists an org's events (the profile page SSR carries none; its events-list callable 401s without a Firebase token), so client-side fetch can't replace the secret ICS; a serverless proxy isn't worth porting the tested parsing for marginal freshness. Public per-event pages *do* expose full data + a CORS-enabled per-event ICS (`calendarFile`) — the sync uses them to backfill ride `image` (see the "Ride photos" bullet), but they are not a feed replacement.
- `PARTIFUL_ICS_URL` **is set**; the sync bot commits real updates every 6h.

## `scripts/fetch_rides.py`

Run on the fixture (never the live feed) with:

```
.venv/bin/python scripts/fetch_rides.py --ics-file tests/fixtures/sample.ics
```

- CLI: `--ics-file PATH` (bypasses the network), `--out PATH` (default `site/events.json`), `--ride-images PATH` (default `scripts/ride_images.json`), `--past-out PATH` (also write the feed's already-happened rides, for `archive_events.py`; both passes share one `now`). With no `--ics-file` it reads `PARTIFUL_ICS_URL`, rewriting a leading `webcal://` to `https://`. **`--ics-file` runs never enrich** — they stay fully offline (zero network calls).
- Importable API for tests: see the function defs and docstrings in `fetch_rides.py`. Injecting `now` pins "future" without depending on the clock.
- **Never echo `requests` exception text** — it embeds the request URL. The fetch path reports only `type(exc).__name__` (+ HTTP status when present). `scrub()` strips URLs from any other surfaced text.
- Output shape: `{"updated_at", "count", "events": [...]}`; each event has `uid, title, start (ISO+offset), end (ISO+offset, may be null), date_display, time_display, location (may be null), location_hidden (bool), location_url (may be null — a maps link pasted in place of an address), description (RSVP/invite line stripped), rsvp_url (may be null), image (may be null), routes (null until enriched, then a list)`. Display strings are precomputed in Python so the page doesn't render in the visitor's tz.
- **`rsvp_url` from the feed text:** Partiful descriptions carry the event page as `RSVP: <url>` / `RSVP at <url>` / `View this event on Partiful at <url>`, line-folded across the phrase/URL boundary — `extract_rsvp_url` matches all three on the unfolded `str(event['DESCRIPTION'])`, never raw lines. `_strip_rsvp` drops the invite line from the displayed description. If there's no link at all, `derive_partiful_url` falls back to `https://partiful.com/e/<uid>` **only** for a bare UID (no `@` — descriptive `<name>@partiful.com` UIDs are never treated as ids). This is what makes the site's "RSVP on Partiful" button link to the actual event.
- **Text cleanup:** `_clean_title` strips the ` | Partiful` suffix (case-insensitive) and collapses whitespace runs, so the site shows the organizer's real name. `_clean_description` (wraps `_strip_rsvp`) additionally trims every line and collapses runs of 3+ newlines into a single paragraph break. Locations are otherwise left alone — see the hidden-location note.
- **Location:** Partiful's event model has a **single** optional Location field — there is no separate start/end location (meeting point vs. café lives in the prose DESCRIPTION instead), so `location` stays the one `LOCATION` value. When the organizer hides the address until RSVP, the export substitutes `Location available once RSVP'd`; `_clean_location` detects it (`HIDDEN_LOCATION_RE`) → `location: null` + `location_hidden: true`, and the site renders "Location shared after you RSVP"; the `.ics` download omits `LOCATION`. A Location field that holds **nothing but a link** is not an address either (2026-08-23) — the organizer sometimes pastes the meeting point's Google Maps URL there (11 archived rides did), and rendered verbatim it is an unbreakable string that overflows the card. `BARE_URL_LOCATION_RE` (`^https?://\S+$`) catches it → `location: null` + `location_url: <url>` (`location_hidden` stays **False**); prose around a link ("Meet at <url>") keeps its whole value as the location. `_clean_location` therefore returns a **3-tuple** `(location, hidden, url)` — three mutually exclusive readings of Partiful's one Location field. The card renders it as a "Meeting point on Google Maps" link and the `.ics`/Google Calendar exports put `Meeting point: <url>` in the **description**, never in `LOCATION` (calendar apps geocode a bare URL badly).
- **Ride photos:** the feed carries no images, so `image` has two sources. (1) **Auto-enrichment** on live-feed syncs only (added 2026-08-13): `enrich_rides()` fetches each ride's public event page at `rsvp_url` (or `derive_partiful_url(uid)`) and extracts the cover image from the page's `__NEXT_DATA__` blob (`props.pageProps.event.image`, a URL string or `{url, blurHash}`), raw Firebase-Storage-URL regex as fallback. Fail-soft: a miss leaves `image: null`, never breaks the sync. (2) The optional sidecar `scripts/ride_images.json` (UID → photo URL); an explicit sidecar entry **wins** over enrichment. A missing file or empty object just means enrichment is the only source; malformed sidecar JSON is a `FeedError` (fails loudly). Verified on `tests/fixtures/event-page.html` with a stub `fetch_page`.
- **Google Maps routes (2026-08-22).** The organizer attaches the ride's route to the Partiful event as a **custom field** — a labelled link (`"Estimated Route"`, `"Team A & C Route"`, `"Start/Bluebikes"`) in `__NEXT_DATA__`'s `event.customFields`, pointing at a `maps.app.goo.gl` short link. `enrich_rides` reads them off the page it already fetches for `image` and calls `rides_routes()`, which resolves each maps-host link and keeps the ones that turn out to be **directions** (`route_from_maps_url` needs both `saddr`+`daddr`, or the `/maps/dir/A/B` path form) — that structural test is what separates a route from a "Start/Bluebikes" pin, rather than pattern-matching the label. Each entry is `{label, url, start, end, via?, points?, mode?, distance_m?, distance_display?}`; `url` is the organizer's **short link**, not the resolved one, because that's what belongs behind a button. A multi-stop route packs its waypoints into `daddr` as `"A to:B to:C"` — `end` is the last one, `via` the rest.
- **Distance is measured, not quoted (2026-08-22).** Google publishes a route's distance only through its billed Directions API: the maps page computes it in JavaScript and the served HTML carries no number (checked — no `mi`/`km` anywhere, `<meta description>` is the generic Google Maps blurb). So the sync measures it from the *same stops Google was given*, via **BRouter** (`brouter.de`, `profile=trekking`) — a keyless public **cycling** router that also handles waypoints. The site renders `~4.0 mi`; the tilde is doing real work, since this won't match Google's figure exactly. Fail-soft: no router, no number, no broken sync.
- **Coordinates come out of the URL's `geocode=` token, not from geocoding.** Each stop has a base64 token in `geocode=t1;t2;…`; each decodes to a tiny protobuf whose field 2 (tag `0x15`) is lat and field 3 (tag `0x1d`) is lon, both `sfixed32 / 1e6`. Verified exact on all 7 real routes, including the two 4-stop team routes. **Don't replace this with Nominatim** — it resolves fewer than half of Google's free-form place strings (every "Bluebikes, Washington St at Temple Pl"-style name comes back empty), and scraping coordinates out of the maps page's minified JS is worse. `maps_geocode_points` returns `[]` unless *every* token parses, and `route_from_maps_url` only keeps `points` when there's exactly one per stop — a partial read must never yield a route that skips a stop.
- **Resolving a `maps.app.goo.gl` link needs a non-browser User-Agent.** It serves a JavaScript Firebase interstitial (HTTP 200, no target anywhere in the HTML) to browser UAs and a plain 302 to everyone else — the sync's `boscafebikers-sync/1.0` gets the redirect chain, so `_resolve_link` works with no browser. Don't "fix" that UA.
- `routes` is `None` until enrichment looks, then a **list** (empty when the event has none). The difference matters: `archive_events.merge_ride` only refuses to overwrite with `None`, so an unenriched re-export can't erase a past ride's known routes.
- Filtering: `start >= now` in `America/New_York`; `STATUS:CANCELLED` dropped. All-day `DATE` values become local midnight.
- The venv here is **Python 3.9**, so the module uses `from __future__ import annotations` for `X | None` hints. Keep that import.

## Fixture contents (`tests/fixtures/sample.ics`)

Partiful-style feed, 4 VEVENTs, deliberately **not** chronological so sorting is exercised. All `DTSTART;TZID=America/New_York` (VTIMEZONE included, so `icalendar` returns tz-aware datetimes directly).

- Future dates are **2030** on purpose, so the fixture doesn't rot.
- Charles carries the ` | Partiful` suffix (exercises `_clean_title`); Minuteman's LOCATION is the real `Location available once RSVP'd` placeholder while Charles has a public address — the shared `rides` fixture exercises `_clean_location` both ways.
- RSVP links end DESCRIPTION as `RSVP: https://partiful.com/e/<id>` after a `\n\n`, **line-folded** across the `RSVP:`/URL boundary — parse the unfolded value (`str(event['DESCRIPTION'])`), never raw lines. (Real exports use the other two phrasings; `extract_rsvp_url` handles all three.)
- Non-ASCII (`é`, `☕`, `→`, `—`) + escaped commas — read the file as bytes, let `icalendar` decode.
- Local venv for verification: `python3 -m venv .venv && .venv/bin/pip install -r requirements.txt` (`.venv/` is gitignored).

## `scripts/archive_events.py` — past rides on the calendar

`site/events.json` is the **upcoming** list; a ride drops out of it the moment it starts. The calendar still shows past rides, so they accumulate in `site/events-past.json`:

```
python scripts/archive_events.py --archive site/events-past.json <source.json> [<source.json> ...]
```

- Sources are events payloads, **oldest first**. Only their already-happened rides are absorbed; the archive's own entries are never re-filtered on `now` (a clock skew must not drop history we already keep).
- Dedupe key: `uid`, else `start|title`. A later source wins field by field **except that `None` never overwrites a value** — that's what keeps a ride's enriched `image` after it moves into the past, since the feed's past export carries no photo and enrichment only runs on the upcoming list. The same rule carries `location_url` — an archive entry that predates the key gains it from a source, and a later export's `null` (or a source with no such key at all) never drops a link already archived; tests pin both directions.
- Rewrites the file only when the `events` list changed (same no-churn guard as `promote_events.py`). Stdlib only; exits nonzero only on a missing/malformed source.
- **The feed carries years of past events** — the first live run archived **34**, back to 2025-09-19, not the 6 that git history had. They arrive from `--past-out` **unenriched** (only the upcoming list is enriched), so `scripts/enrich_archive.py` fills them in **newest-first, `--limit 8` per sync**: enriching 34 event pages every 6 hours would be pointless traffic for data that never changes. `routes is None` is the "never checked" marker it selects on, so once the archive is filled the step is a no-op. Result today: 34/34 have an image, 7 have route links (older rides simply had no route custom field).
- **The archive must accumulate** — don't "simplify" it into a plain past-events dump from the feed. Partiful gives no guarantee it keeps exporting old events, and once one falls out of the feed the archive is the only copy.
- `fetch_rides.py --past-out PATH` writes the feed's already-happened rides for it; `parse_events(..., past=True)` is the same parse with the filter flipped.
- Seeded 2026-08-22 from the 23 historical versions of `site/events.json` in `git log` (6 rides back to 2026-07-25), then re-cleaned through `_clean_title`/`_clean_description` and enriched — the four oldest predate the text-cleanup code, so they carried ` | Partiful` titles, the invite line, and `rsvp_url: null`.

## Route maps (`scripts/route_map.py`, `scripts/render_route_maps.py`)

Each ride's card banner is a **drawn map of its route**, not the Partiful poster (2026-08-22). `render_route_maps.py` asks BRouter for the route geometry, `route_map.py` projects it to Web Mercator and emits an SVG in the café palette, and the ride gets `map_image: "maps/<uid>.svg"`.

- **Why drawn and not a screenshot.** No browser in the sync to screenshot with; Google's Static Maps API needs a billed key; hotlinking a public tile server would aim every visitor's browser at someone else's infrastructure. So the route is drawn as SVG — and since 2026-08-23 the basemap is raster tiles embedded as data URIs in that same SVG (next bullets).
- **Basemap (2026-08-23).** `render_route_maps.py` fetches **OpenStreetMap's standard tiles** (`https://tile.openstreetmap.org/{z}/{x}/{y}.png`, 256px, keyless; UA `boscafebikers-sync/1.0`) at sync time, 2–16 per *new* ride, and `route_map.py` embeds them as `data:` `<image>`s in one `translate/scale` `<g>` in the same Mercator projection (integer tile positions inside one transform — per-tile fractional offsets leave hairline seams). Never hotlinked by the visitor's browser. **Provider is a licensing decision, not a URL:** the tiles are *stored* in committed SVGs. CARTO Positron was tried first and rejected — its Basemap Terms (§8, §9.3) require an API key and forbid storing/redistributing tile content, including as static images, so a key would not have helped. OSM's data is ODbL and its standard style is public domain; the tile usage policy asks for a unique UA, attribution clearly on the map, and no bulk downloading — the sync meets all three (a test pins the URL host + UA). osm.org/copyright does not state a separate licence for the rendered tile images, so keep the volume tiny and the credit visible. Credit line reads "Map © OpenStreetMap contributors" (`TILE_CREDIT` is `""`; the `tile_credit` param appends a provider's own line if one is ever needed), drawn at `CREDIT_SIZE` = 16 units (12 was ~4px on a phone). Tiles sit under a `WASH_OPACITY` (.42) latte wash + a bottom latte fade; labels get a foam `paint-order: stroke` halo; route casing is FOAM over tiles (OAT on the plain fallback).
- **`route_map.py` is still stdlib-only and network-free:** `tile_plan(geometry)` → `(zoom, [(x, y)…])`; the caller fetches and passes `tiles={(z, x, y): bytes}` to `render_route_svg(..., tiles=, tile_source=, tile_credit=)`, which recomputes the same plan. `fit()` exposes the projection `(scale, offset_x, offset_y, min_x, min_y)` that both `project()` and the tiles use. Tests stub `fetch_tile` with `tests/fixtures/tile.png`; a synthetic 256px palette PNG is built in-test for the crop.
- **Bytes: edge tiles are cropped, zoom follows tile side.** OSM tiles are ~30KB each and only ~40–45% of an edge tile is on the canvas, so `crop_png()` (inflate → unfilter → slice → filter-0 rows + zlib-9; OSM's tiles are 8-bit palette, non-interlaced, filter 0, sometimes several IDAT chunks — joined) trims them; anything exotic (16-bit, interlaced, not 256×256) is embedded whole. With cropping, bytes ∝ 1/(tile side)², so `choose_zoom` picks the deepest zoom with side ≥ `MIN_TILE_SIDE` (320 canvas px) — one zoom deeper = 4× bytes. Today: 54–288KB per map, ~1.09MB for all nine (plain maps were 6–19KB); every new ride adds 55–290KB committed forever. **`MIN_TILE_SIDE` is the one knob** — 400–450 roughly halves the bytes at the cost of softer tiles on desktop. Don't add PIL — the crop is deliberately stdlib.
- **Fail-soft is all-or-nothing and idempotency keys on a marker.** Any missing/non-image tile → the plain latte drawing (byte-identical to the pre-basemap output) with **no** `data-basemap` attribute; `render_for_ride` skips an existing map only when `route_map.has_basemap()` sees `data-basemap="osm-standard"` on the root, so an unfinished map is retried each sync (re-fetching its 2–16 tiles until the outage clears) and rewritten only when the output differs. `--redraw` forces every map (used 2026-08-23 to regenerate the nine). The CLI prints which maps are still without a basemap.
- **Labels mirror the card (2026-08-23).** The bottom-left label uses `_start_name()` — the same rule as `ride-card.js` `startName()` — so a Bluebikes dock reads "Start · Cleveland Circle", not "Start · Bluebikes"; the end label keeps `_place_name()` ("O'Some Café · End"). Keep the Python and JS rules in step. Attribute values (`aria-label`, `data-basemap`) go through `_attr()`: `xml.sax.saxutils.escape` alone leaves `"` unescaped, which made the root tag malformed for a title containing a quote.
- `site/maps/*.svg` are **committed**, and the render step is **idempotent** — a ride whose SVG exists *with a basemap* is skipped and only re-pointed at it, so the 6-hour sync draws only genuinely new rides.
- **The canvas shape follows the route** (`canvas_size`): height/width clamped to `[0.45, 1.0]`, so a north-south ride gets a near-square frame instead of a hairline down the middle of a letterbox. The card therefore renders map images with **`object-fit: contain`** (`.ride-img.is-map`), not the `cover` a photo gets — cropping a map loses its endpoints. Since the tiles fill the canvas the rule is `width: auto; max-width: 100%; max-height: 420px; margin: 0 auto; border-radius: 10px` (2026-08-23): full width on a phone, a tall map shrinks to 420px and sits centred on the oat card at desktop — no latte letterbox bars.
- The distance badge sits **top-left**, not centred at the bottom: a long café name in the "… · End" label ran straight over it there. `fit_labels()` clips the two bottom labels until their estimated widths fit side by side.
- One scale for both axes — never stretch to fill, that would be a map that lies. `PAD_TOP` (66) reserves the badge's corner; `PAD_BOTTOM` (74) the label row.
- Feed text (title, place names) goes through `xml.sax.saxutils.escape` — same no-injection rule as the DOM.
- ODbL: the geometry is derived from OpenStreetMap, so every map carries "Route © OpenStreetMap contributors".
- `sync.yml`'s commit step **stages before diffing** (`git add -A … && git diff --cached --quiet`): the maps are *new* files and a plain `git diff` doesn't see untracked ones.
- No rasterizer here, but `qlmanage -t -s 900 -o <dir> map.svg` renders an SVG to PNG so a map can actually be looked at.

## `.github/workflows/sync.yml`

Cron `0 */6 * * *` (UTC) + `workflow_dispatch`. Ubuntu, Python 3.11, pip cache keyed on `requirements.txt`, `concurrency: sync-rides` so two runs can't race a push. `sync` job carries `permissions: contents: write`; the `deploy` job carries the Pages permissions.

- **Order matters: archive → backfill → draw maps → promote.** `render_route_maps.py` runs on the **scratch** `$RUNNER_TEMP/events.json`, *before* the promote. It adds `map_image`, which a fresh fetch never carries, so running it after the promote leaves the committed file permanently unequal to any fetch and **every run commits a fresh `updated_at` forever** — the exact churn `promote_events.py` exists to stop. (Caught by running the sync twice and diffing the bot's commit.) Same reasoning as the scratch-file rule below: keep them.
- **Order matters: archive, then promote.** `archive_events.py` runs while `site/events.json` still holds the *previous* upcoming list, so a ride that has since started gets archived even if Partiful already dropped it from the feed. The commit step stages **both** `site/events.json` and `site/events-past.json` and its `git diff --quiet` guard covers both.
- **The commit-if-changed guard needs `promote_events.py`.** `build_payload()` stamps a fresh `updated_at` on every run, so `git diff` on `site/events.json` would always be dirty. The workflow writes the fetch to `$RUNNER_TEMP/events.json`, then `scripts/promote_events.py <new> site/events.json` copies it into place only when the `events` lists differ; the last step is `git diff --quiet -- site/events.json`. **Don't "simplify" this back into a single fetch-in-place step.**
- `promote_events.py` is stdlib-only, exits 0 whether or not it copied, nonzero only if the new file is missing/unparseable. No tests (verified by hand).
- Committer identity is the `github-actions[bot]` noreply address.
- The secret is passed as `env: PARTIFUL_ICS_URL: ${{ secrets.PARTIFUL_ICS_URL }}` on the fetch step only — never as a CLI arg (args show up in logs).
- No `yaml` module in the venv; validate the workflow with `ruby -ryaml -rjson -e 'puts JSON.pretty_generate(YAML.load_file(".github/workflows/sync.yml"))'`.

## Status

Phase 1 (build) and phase 2 (deploy) are complete — the site is live and the sync→deploy freshness chain works. Phase 3 (Backlog 3) is in progress. Historical per-iteration notes live in `ralph/ralph.log` (gitignored); `git log` records what each iteration changed.

## `site/index.html`

No build step: `site/styles.css` in `<head>` (shared base — palette, nav, hero, footer), a small page-specific inline `<style>`, and the ride JS as three plain `defer`'d scripts at the end of `<body>` — `js/ride-card.js`, `js/calendar.js`, `js/app.js` — each an IIFE attaching its public API to the shared `window.BCB` namespace (no module system or build step). The `defer`'d FullCalendar CDN script sits in `<head>`; all four defer, so FC executes before the ride scripts in document order on a real page and the calendar takes the FullCalendar path directly, falling back to the hand-rolled grid only when the CDN is unavailable. The node DOM-shim loads the same three files in the same order (see "Verifying the JS").

- **Shared tab header nav.** All six pages carry the same `.nav` — an espresso bar with **four** relative `.tabs` links (Rides / Gallery / Contact / Donate). Shop and Meta Business were removed from the top tabs (2026-08-13; keep on footer) — reachable only from the `.footer-nav`, so `shopify.html`/`meta-business.html` have **no** `.is-active` in the top nav. The nav is **static and scrolls with the page** — the organizer asked for "not stay permanently on screen", so no `position: sticky` (2026-08-13). The current page's link gets `.is-active` + `aria-current="page"`. Nav CSS lives once in `styles.css`; each page keeps only its page-specific rules inline. Links are relative, never root-relative.
- **Index hero is compact (index only, 2026-08-13).** `index.html`'s inline `<style>` overrides the shared tall `.hero` (smaller paddings, `2rem` mark, `clamp(1.7rem, 7vw, 2.4rem)` h1, `1rem` tagline, ~35% shorter). Sub-pages keep the shared hero. There is **no hero CTA button**, so the tagline's shared `margin: 0` stands — don't restore a `margin-bottom` for one.
- **Shared footer.** All six pages end with the same `<footer>`: `.footer-brand` tagline ("…exploring the city one café at a time"), a `.footer-nav` carrying **all six** links (Rides / Gallery / Shop / Meta Business / Contact / Donate — relative, no `.is-active`), and a `.footer-social` row (Instagram + Partiful, `.sep` separated). Shop and Meta Business are kept here — the footer is their only nav home. Footer styles live once in `styles.css`; the sub-pages' footers stay identical to home's.
- **Palette:** CSS custom properties on `:root` are the warm café palette (`--espresso`, `--roast`, `--crema`, `--latte`, `--foam`, `--oat`, `--ink`, `--muted`). Reuse them; don't introduce new hex values.
- **Layout:** mobile-first — `.wrap` (max-width 680px, 20px gutters) and one `@media (min-width: 560px)` block that only bumps vertical padding. Readable at 380px.
- **Desktop typography (2026-08-13):** base `body` font is `1rem`; a `@media (min-width: 760px)` block in `styles.css` bumps the **root** font to `18px` (~12.5% scale-up on desktop, so every rem-based size scales). Keep `body` font-size as `1rem`, never a px literal. Mobile (below 760px) is byte-for-byte unchanged.
- **Photos load last (`data-bg`), backing one section.** Photos are *not* `background-image`s in the stylesheet — `app.js` (`applyBackgrounds()`) reads the `data-bg` attribute on matching elements and copies it into `background-image` only after `window.load`, adding `.bg-loaded`, so a photo never blocks first paint. **Only `#next-ride` is photo-backed** (café photo `images/jess-b-gracies-bikes.jpeg`): the organizer asked for no background images below the calendar, so `#first-ride`, `#about` and `#crew` lost their `data-bg` (2026-08-22) and now read on the plain latte page background with default ink text, exactly like `#rides`. The generic `[data-bg]` CSS stays — it's what makes the one remaining section work, and re-adding an attribute is all a future photo section needs. The espresso gradient in `styles.css` is the no-JS / pre-load fallback on every `[data-bg]` element (the hero keeps the same gradient, permanently photo-free — never carries `data-bg`). `.bg-loaded` adds `background-size: cover` / center + a translucent espresso `::before` overlay (the section's `.wrap` is lifted above it via `position: relative; z-index: 1`). Overlay opacity is the **`--bg-overlay-opacity`** custom property (default **`.7`**) — the readability "setting": raise to darken, lower to show more photo. Text over photos: `[data-bg] h2` = foam; `.note`, `:not(.ride) > p`, `ul.checks li` = oat (`strong` = foam) — each with a subtle espresso text-shadow. The `:not(.ride)` scope is deliberate: the featured ride card is an opaque oat card whose `<p>` must stay dark. Ride `<img>`s stay `loading="lazy"`.
- **Rendering script** (`app.js`, loads last; `ride-card.js`/`calendar.js` supply helpers via `window.BCB`): `fetch("events.json")` →
  - **Next-ride section** (`#next-ride`, first section in `<main>`, under the photo-free hero): `setNextRide()` renders `events[0]` (events.json is start-sorted and the sync filters to future) into `#next-ride-card` via the shared `rideCard(ev, "next-ride")`; nothing upcoming → note + "See all rides on Partiful" button.
  - **Schedule section** (`#rides`) is **calendar-only** — the list view and its `#view-toggle` were removed in favor of the next-ride card. `<div id="schedule">` gets the month calendar (FullCalendar 6 primary, grid fallback); `<p class="note" id="updated">` holds the "Last updated … ET." stamp as a clickable link (`#updated-link`) to the `sync.yml` workflow — the "Sync rides now" button is gone. The line stays `hidden` until `updated_at` renders (failed load leaves no empty focusable link). Zero events / non-OK response / bad JSON → note + Partiful button pointing at the profile URL.
  - **Card banner:** `map_image` (the drawn route map) wins over `image` (the Partiful poster). A map banner links to the **route** and opens in a new tab; a poster links to RSVP. `.ride-img.is-map` switches to `object-fit: contain` and keeps the image's own shape (`width: auto; max-height: 420px`, centred).
  - **`rideCard(ev, extraClass)`** renders one `.ride` card: `.when` = `date_display · time_display`, `.where` = location or "Location shared after you RSVP" when `location_hidden` — or, when `location_url` is set instead of `location`, a **link** reading "Meeting point on Google Maps" (`meetingPointText()` falls back to plain "Meeting point" for a non-Google host; the match is a regex on the URL string, not `new URL`, so the node shim needs no extra global); `.ride .where` carries `overflow-wrap: anywhere` so no unbreakable location can push the card wide again; both exports share `exportDetails(ev)` — description, then `Meeting point: <url>` when there is one, then `RSVP: <url>` — so the `.ics` DESCRIPTION and the Google Calendar `details` can't drift, and the ICS `LOCATION` line / gcal `location` param stay omitted for a link-only location, a `.route` line per entry in `routes` (the host's Google Maps link, `target="_blank"`, a `.route-distance` pill when `distance_display` is set, plus a `.route-start` span "from <start>" — **start only, no end, no stops note** (2026-08-23; the organizer found "Start → End" redundant). `startName()` derives it: rides start at Bluebikes docks whose address is "Bluebikes, <station>, <city>, MA <zip>", so the leading "Bluebikes" segment is dropped and the station detail shown ("Bluebikes, Cleveland Circle, Boston, MA 02135" → "Cleveland Circle"; a two-segment station like "Bunker Hill Mall, Main St at Austin St" is kept whole by stripping the trailing "<city>, <ST> [zip]" pair; bare "Bluebikes" falls back to the whole string); any other start keeps its leading comma-segment (`placeName()`). The untrimmed "start → end" pair stays in the link's `title`; `BCB.startName` is exported for the node shim, and `route_map._start_name` is its Python twin for the map label, a `<p>` description, and `<a class="btn">RSVP on Partiful` (missing `rsvp_url` → profile URL). With `image`, the card starts with a `.ride-img-link` banner wrapping `<img class="ride-img" loading="lazy">` (alt = ride title) linked to the RSVP target; omitted entirely when no image. Each card ends with `.ride-actions`: the RSVP button plus `btn btn-ghost` "Add to calendar" (browser-generated per-event `.ics` via `buildIcs`/`downloadIcs` — `DTSTART`/`DTEND` use the Eastern wall-clock ISO prefix as `;TZID=America/New_York:…`, no `new Date(start)`; long lines folded per RFC 5545 `foldIcsLine`, text escaped `icsEscape`, RSVP URL appended + emitted as `URL:`) and `btn btn-ghost` "Google Calendar" `<a>` (new tab) mirroring the BCU export — `googleCalUrl(ev)` builds a `calendar.google.com/calendar/render?action=TEMPLATE` URL from the same Eastern wall-clock fields (`ctz=America/New_York`; `end` null → +1h via `icsDateTimePlusHour` Date.UTC arithmetic so a near-midnight ride rolls into the next day). The action row is **mobile-first stacked**: below 560px the three `.btn`s are full-width (`flex: 1 1 100%`), so the primary CTA spans the card; `@media (min-width: 560px)` restores compact inline pills (`flex: 0 1 auto`).
  - **Ride-detail modal** (`#ride-modal`, `.modal` overlay with `.modal-backdrop` + `.modal-dialog`): clicking a calendar event (FullCalendar `eventClick` or a fallback `.ride-chip`) opens it instead of navigating to RSVP. Content is built by the *same* `rideCard(ev, "modal-ride")` builder as the featured card, so details and exports can't drift; `.modal .ride` drops the card chrome so the dialog is the single container. `openRideModal(ev)` stores `document.activeElement`, sets the dialog's `aria-label` to the ride title, hides body scroll, focuses the close button; `closeRideModal()` restores focus to the opener. Closing: the `×` button, Escape (`keydown`), or a backdrop click (target not inside `.modal-dialog`). Modal at `z-index: 100` (above the nav), relies on `[hidden]` to beat `display: flex`. The dialog is a **flex column** with `#ride-modal-content` as its scrollport (`min-height: 0; overflow-y: auto`) within `max-height: 88vh`, so the pinned 44px `.modal-close` stays visible while a long description scrolls — don't revert to `overflow-y: auto` on the whole dialog.
- **Past rides are on the calendar** (2026-08-22). `app.js` fetches `events-past.json` alongside `events.json`; a 404 is fine (fail-soft to no past rides), but a failed `events.json` stays an error rather than falling back to the archive — "here are last month's rides" would read as upcoming. Each archived ride is **copied** with `past: true`, which drives: a dimmed `.is-past` chip / FC `classNames`, and a `rideCard` variant with a "Past ride" pill on the `.when` line and a single "See it on Partiful" action (no RSVP wording, no .ics / Google Calendar — the ride is over). The next-ride card only ever reads the upcoming list. `renderCalendarFull(events, mountEl, focusIso)` takes the focus date explicitly: `events[0]` is now the **oldest archived** ride, so app.js passes the next upcoming start (or the newest past one when nothing is upcoming) — otherwise the calendar opens months in the past.
- **Calendar view** — the schedule section's only renderer. **Primary: FullCalendar 6** (`dayGridMonth`): when `window.FullCalendar` is defined, `renderCalendarFull(events, schedule)` builds a `FullCalendar.Calendar` in `#ride-calendar` with `timeZone: "America/New_York"`, `initialDate` = first ride's Eastern date, `height: "auto"`, `dayMaxEvents: 3`, `eventDisplay: "block"`, prev/next header nav, and one event per ride (`title`, `start`, `end`, `url` = `rsvp_url`→profile fallback, `extendedProps.data` = the full event). **`displayEventTime: false`** (2026-08-23): on a ~45px phone chip FC's leading time prefix ate the whole chip and the title never showed ("1 O…"), so the time appears only on the ride card and in the modal, never on a chip. `eventClick` intercepts the left-click and opens the modal instead of navigating; the event keeps its `url` so middle/ctrl-click still opens RSVP in a new tab (native anchor behavior). `render()` calls `destroyCalendar()` first so a stale instance can't leak. **The box must be in the document before `FullCalendar.render()`** — `renderCalendarFull` appends the `.calendar` box into `mountEl` first, because FC measures its container via `getBoundingClientRect()` at mount, and a detached holder measures 0-wide and crushes every event to ~14px (the "calendar looks completely broken" bug). Don't revert to build-then-append. **Fallback:** CDN unavailable → `typeof FullCalendar` undefined → hand-rolled `groupByMonth()` / `monthGrid()`: one `.calendar` block per month, 7 `.cal-dow` weekday columns, a `.num` per day cell, one `.ride-chip` `<button>` per ride (opens the same modal; CSS resets the UA button look — `font: inherit; border: 0; text-align: left; cursor: pointer; width: 100%`), `.cal-day.has-ride` tints days. **Mobile cell heights (2026-08-13):** below 560px ride titles truncate to one line (`white-space: nowrap` + `text-overflow: ellipsis` on `.ride-chip` and `.fc .fc-event-title`) instead of wrapping, and every tappable calendar element (`.ride-chip`, `.fc .fc-daygrid-event`, the FC `+N more` link) drops from `min-height: 44px` to `28px` so a wrapped long name can't stretch a mobile column. Two supporting rules make this safe: `.cal-day { min-width: 0 }` (a grid item's automatic minimum would otherwise force the whole calendar wider than the viewport when the chip is nowrap) and `.fc .fc-daygrid-event .fc-event-main { min-width: 0 }` (lets FC's flex child shrink so the title ellipsizes). `@media (min-width: 560px)` restores `min-height: 44px` + wrapping titles (`white-space: normal; overflow-wrap: anywhere`). The full title is always one tap away in the modal (and the chip's `aria-label`). A tiny hook listens for the CDN script's `load` event and re-renders if it lands after an earlier fallback render.
- **Calendar display decision:** FullCalendar 6.1.15 via its `index.global.min.js` global build (MIT, zero build step, the only maintained/no-build candidate); theme it through the `--fc-*` custom properties overridden with `!important` (the library injects its `<style>` later). Rejected alternatives + sizing in ralph.log. The hand-rolled grid is kept as the zero-dependency fallback for a CDN outage.
- **Calendar weekday math is the exception to the no-`Date` rule.** The calendar must not call `new Date(start)` in the visitor's tz — a near-midnight ride would land on the wrong weekday for non-Eastern visitors. The `start` ISO string already carries the Eastern offset, so its Y-M-D prefix *is* the Eastern wall-clock date; the code builds `new Date(Date.UTC(y, m-1, d))` and reads `getUTCDay()` / `getUTCDate()` — the same weekday in any timezone. Display times still come from the precomputed `date_display`/`time_display`.
- **`[hidden]` needs `display: none !important`** in the CSS — global safety net so no `display: flex` can override the UA's `hidden` rule.
- `events.json` display strings are precomputed; the JS must **not** re-format dates with `Date`, or visitors outside Eastern see wrong times. `updated_at` is likewise formatted by **regex-slicing the ISO string** (it already carries the Eastern offset) — keep it that way.
- The DOM is built with `createElement`/`textContent`, never `innerHTML`, so feed text can't inject markup. Keep that.
- **JS module split (2026-08-13):** three plain scripts in dependency order — `ride-card.js` → `calendar.js` → `app.js` — each an IIFE attaching its public API to `window.BCB`, all `defer` after the FullCalendar CDN script. Chosen over native ES modules: both need zero build step and Pages serves either, but plain scripts keep the node DOM-shim's `vm.runInContext` verification trivial and the cross-file references resolve at call time. `window.BCB` is the one shared global — never leak anything else. Dependency-order contract: `ride-card.js` owns the constants (`PARTIFUL`, `MONTHS`, `DOW`), `el()`, `pad2()`, the `rideCard` builder, and `buildIcs`/`downloadIcs`/`googleCalUrl`; `calendar.js` owns the calendar renderers and Eastern date math (`renderCalendarFull(events, mountEl)` appends its box into `mountEl` before `FullCalendar.render()`; the fallback `monthGrid()` returns a box `app.js` appends itself); `app.js` owns DOM refs, `currentData`, the modal, and the bootstrap.
- **Verifying the JS:** no browser here, but `node` (v25+) is installed. The node DOM-shim `readFileSync`s `ride-card.js`, `calendar.js`, `app.js` (in that order) and evals each against a shimmed `document`/`fetch`/`window` (no regex-pulling out of the HTML) — `window` points at the vm global so `window.BCB` survives across files. It checks the happy path, empty, missing-rsvp, 404, the ride-detail modal (open via chip / FC `eventClick`, close via backdrop / Escape / `×`, stale instance destroyed on re-render), and the deferred `data-bg` background (unset before the window `load` event, set after — the shim fires `context.handlers.load()` to prove it).
- Sections/ids: `#next-ride`, `#rides`, `#first-ride`, `#about`, `#crew`; only `#next-ride` carries `data-bg`. The ride-detail overlay is `#ride-modal` (not a `<section>`). `#about` absorbed the former `#links` "Find us" section — there is no `#links` anymore.
- **Crew section** (`#crew`, home page only) — the old `#contact` section, rewritten 2026-08-22 once the contact copy moved to `contact.html`. "Help Café Bikers behind the scenes": ride leaders, sweepers, and the people filming/editing. It keeps the **same three** `.links` cards the contact section had — email `mailto:boscafebikers@gmail.com`, Instagram DM (`https://instagram.com/bostoncafebikers`), and the WhatsApp group invite (`https://chat.whatsapp.com/JtpmhMgE8EmFRGOXrNhT1w`). Same email on the donate page's WIP note. There is no `#contact` section on index anymore; sub-pages other than `contact.html` have none either — only the shared footer.
- Verified well-formed by feeding it through `html.parser` (no unclosed or mismatched tags).

## `site/contact.html`

Its own page as of 2026-08-22; the slot it vacated on `index.html` is now the `#crew` section. Same shell as the other sub-pages (shared nav/hero/footer + page-specific inline CSS), plus a real email form and `js/contact.js` (`defer`, standalone — it does **not** touch `window.BCB`).

- **Sending, on a static site.** There is no server to POST to and nowhere to keep an API key, so "send" composes a `mailto:` and hands it to the visitor's own mail client. `contact.js` intercepts `submit`, builds `mailto:<addr>?subject=…&body=…` (message plus an `— Name <reply-to>` signature) and assigns `window.location.href`. Native `mailto:` form submission mangles the body in most browsers, which is why JS takes over.
- **Changing the address: edit `action="mailto:…"` on `#contact-form` and nothing else.** That attribute is the single source — `contact.js` parses the address out of it for both the prefilled `mailto:` and the "Prefer your own mail app? Write to …" fallback link (`#contact-direct`), and with JS off the browser still submits to that same action. Currently `ecao.csindie@gmail.com` (the organizer's, not the group's `boscafebikers@gmail.com`, which stays on the "Other ways to reach us" card).
- `#contact-direct-line` and `#contact-status` start `hidden` and are revealed by JS — no empty focusable link when JS is off (same pattern as `#updated` on the rides page).
- **Both buttons share one build (2026-08-23).** The `← Back to the ride page` link is `.btn.btn-back`; `.btn-back` rides on the `.contact-form .btn` rule (inline-flex, `min-height: 44px`, full-width below 560px / pill above — the ride-card action-row convention) and adds `margin-top: 1rem`, because `.links` is a flex box with no bottom margin and the shared inline-block `.btn` sat flush against the last card (the "isn't vertically aligned" bug). Don't drop the class back to plain `.btn`.
- Fields: name, email (`type="email"`), subject (optional → defaults to "Hello from the Boston Café Bikers site"), message. Native `required` does the validating; inputs are `font: inherit` at ≥16px so iOS Safari doesn't zoom on focus, and every control clears 44px.

## `tests/test_fetch_rides.py`

Run with `.venv/bin/python -m pytest tests/ -q` (199 passing, incl. `tests/test_archive_events.py`, `tests/test_route_map.py`, `tests/test_enrich_archive.py`).

- No `conftest.py` / packaging; the test file puts `scripts/` on `sys.path` itself and does `import fetch_rides`.
- Pinned clock: `NOW = 2025-01-01 12:00 America/New_York`, passed as `parse_events(..., now=NOW)`. Never call the parser without `now` in a test.
- Coverage: the full suite in `tests/test_fetch_rides.py` (incl. leak tests that the feed URL never reaches an error message).
- The suite passes under any system timezone (verified with `TZ=Asia/Tokyo`) — keep it that way; assert on explicit offsets, not on local time.
